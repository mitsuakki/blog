name: Spell check

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  spellcheck:
    name: Check spelling
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: crate-ci/typos@master
        with:
          config: .typos.toml
